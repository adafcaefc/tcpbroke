# Server (the public relay). Deployed as a container; see Dockerfile.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
# >=14 for websockets.asyncio, and for `additional_headers` on the client used by the CLI.
websockets>=14,<16
